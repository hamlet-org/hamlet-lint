name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel stale runs on the same branch or pull request. This also keeps the
# dependency image publisher ordered with the newest commit.
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: read

env:
  OCAML_COMPILER: "5.5.0"
  MINIMUM_DUNE: "3.18.2"
  # ci pins the private companion source explicitly. public pin-depends are
  # for normal consumer installation and must not override that checkout.
  OPAMIGNOREPINDEPENDS: "true"
  # development CI follows the companion Hamlet main branch. releases use
  # exact published pairs.
  HAMLET_GIT_REF: main

jobs:
  classify:
    runs-on: ubuntu-latest
    outputs:
      docs_only: ${{ steps.changes.outputs.docs_only }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - id: changes
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base="${{ github.event.pull_request.base.sha }}"
          else
            base="${{ github.event.before }}"
          fi

          if [ -z "$base" ] || [ "$base" = "0000000000000000000000000000000000000000" ]; then
            echo "docs_only=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if ! git cat-file -e "${base}^{commit}" 2>/dev/null; then
            git fetch --no-tags --depth=1 origin "$base"
          fi

          git diff --name-only "$base" "$GITHUB_SHA" > changed-files.txt

          docs_only=true
          while IFS= read -r path; do
            case "$path" in
              *.md|LICENSE|knowledge/*|.github/dependabot.yml) ;;
              *) docs_only=false ;;
            esac
          done < changed-files.txt

          echo "docs_only=${docs_only}" >> "$GITHUB_OUTPUT"

  # Resolve the source pin once. The resulting commit is part of the image
  # key: a moving ref such as `main` must never reuse dependencies compiled
  # against an older Hamlet or ppx_hamlet revision.
  resolve-hamlet:
    runs-on: ubuntu-latest
    needs: classify
    if: needs.classify.outputs.docs_only != 'true'
    outputs:
      git_commit: ${{ steps.resolve.outputs.git_commit }}
    steps:
      - name: Checkout exact Hamlet source
        id: checkout
        continue-on-error: true
        uses: actions/checkout@v6
        with:
          repository: hamlet-org/hamlet
          ref: ${{ env.HAMLET_GIT_REF }}
          token: ${{ secrets.HAMLET_READ_TOKEN || github.token }}
          path: hamlet-source
          fetch-depth: 1
          persist-credentials: false

      - name: Explain Hamlet source access failure
        if: steps.checkout.outcome != 'success'
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::CI needs read access to hamlet-org/hamlet at ${HAMLET_GIT_REF}. Configure the optional HAMLET_READ_TOKEN secret with repository read access, or grant this workflow token read access to the companion repository."
          exit 1

      - id: resolve
        shell: bash
        run: |
          set -euo pipefail
          commit=$(git -C hamlet-source rev-parse HEAD)
          echo "git_commit=$commit" >> "$GITHUB_OUTPUT"

  ensure-ci-image:
    runs-on: ubuntu-latest
    needs: resolve-hamlet
    if: needs.resolve-hamlet.result == 'success'
    permissions:
      contents: read
      packages: write
    outputs:
      image: ${{ steps.resolve.outputs.image }}
    steps:
      - uses: actions/checkout@v6

      - name: Checkout exact Hamlet source
        uses: actions/checkout@v6
        with:
          repository: hamlet-org/hamlet
          ref: ${{ needs.resolve-hamlet.outputs.git_commit }}
          token: ${{ secrets.HAMLET_READ_TOKEN || github.token }}
          path: hamlet-source
          fetch-depth: 1
          persist-credentials: false

      - name: Resolve CI image
        id: resolve
        shell: bash
        env:
          SOURCE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          HAMLET_GIT_COMMIT: ${{ needs.resolve-hamlet.outputs.git_commit }}
        run: |
          set -euo pipefail
          image_name="${GITHUB_REPOSITORY,,}-ci"
          short_sha="${SOURCE_SHA:0:12}"
          deps_hash=$(
            {
              sha256sum dune-project ./*.opam .github/docker/ci/Dockerfile
              printf 'ocaml=%s\nhamlet-commit=%s\n' \
                "$OCAML_COMPILER" "$HAMLET_GIT_COMMIT"
            } | sha256sum | cut -c1-16
          )

          candidate="ghcr.io/${image_name}:deps-${deps_hash}"
          fallback="ghcr.io/${image_name}:main"
          can_publish=false
          if [ "${{ github.event_name }}" != "pull_request" ] \
            || [ "${{ github.event.pull_request.head.repo.full_name }}" = "${{ github.repository }}" ]; then
            can_publish=true
          fi

          echo "${{ secrets.GITHUB_TOKEN }}" \
            | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

          needs_build=false
          image="$candidate"
          if docker manifest inspect "$candidate" >/dev/null 2>&1; then
            echo "using existing ${candidate}"
          elif [ "$can_publish" = "true" ]; then
            needs_build=true
            echo "will build ${candidate}"
          else
            image="$fallback"
            echo "fork pull request cannot publish ${candidate}; falling back to ${fallback}"
          fi

          if [ "$needs_build" != "true" ] && ! docker manifest inspect "$image" >/dev/null 2>&1; then
            echo "::error::CI image not found: $image"
            exit 1
          fi

          {
            echo "build_tags<<EOF"
            echo "$candidate"
            echo "ghcr.io/${image_name}:sha-${short_sha}"
            if [ "${{ github.ref }}" = "refs/heads/main" ]; then
              echo "ghcr.io/${image_name}:main"
            fi
            if [ "${{ github.event_name }}" = "pull_request" ]; then
              echo "ghcr.io/${image_name}:pr-${PR_NUMBER}"
              echo "ghcr.io/${image_name}:pr-${PR_NUMBER}-${short_sha}"
            fi
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          echo "image=${image}" >> "$GITHUB_OUTPUT"
          echo "needs_build=${needs_build}" >> "$GITHUB_OUTPUT"

      - if: steps.resolve.outputs.needs_build == 'true'
        uses: docker/setup-buildx-action@v4

      - name: Build and publish CI image
        if: steps.resolve.outputs.needs_build == 'true'
        uses: docker/build-push-action@v7
        with:
          context: .
          file: .github/docker/ci/Dockerfile
          push: true
          cache-from: type=gha,scope=hamlet-subtractor-ci-image
          cache-to: type=gha,mode=max,scope=hamlet-subtractor-ci-image
          build-args: |
            OCAML_VERSION=${{ env.OCAML_COMPILER }}
            HAMLET_GIT_COMMIT=${{ needs.resolve-hamlet.outputs.git_commit }}
          tags: ${{ steps.resolve.outputs.build_tags }}
          labels: |
            org.opencontainers.image.title=hamlet-subtractor ci
            org.opencontainers.image.description=OCaml CI image for Hamlet Subtractor build, test, docs, format, and opam checks

  build-ubuntu:
    name: build (ubuntu-latest, 5.5.0)
    runs-on: ubuntu-latest
    if: needs.classify.outputs.docs_only != 'true'
    needs:
      - classify
      - resolve-hamlet
      - ensure-ci-image
    container:
      image: ${{ needs.ensure-ci-image.outputs.image }}
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v6

      - name: Checkout exact Hamlet source
        uses: actions/checkout@v6
        with:
          repository: hamlet-org/hamlet
          ref: ${{ needs.resolve-hamlet.outputs.git_commit }}
          token: ${{ secrets.HAMLET_READ_TOKEN || github.token }}
          path: hamlet-source
          fetch-depth: 1
          persist-credentials: false

      - run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$GITHUB_WORKSPACE/hamlet-source"

      - name: Ensure exact Hamlet source dependencies
        shell: bash
        run: |
          set -euo pipefail
          opam pin add -y --no-action hamlet "$GITHUB_WORKSPACE/hamlet-source"
          opam pin add -y --no-action ppx_hamlet "$GITHUB_WORKSPACE/hamlet-source"
          opam install -y hamlet ppx_hamlet
          opam install -y --ignore-pin-depends . --deps-only --with-test --with-doc --with-dev-setup

      - run: make all

  build-dune-minimum:
    name: build (ubuntu-latest, Dune 3.18.2)
    runs-on: ubuntu-latest
    if: needs.classify.outputs.docs_only != 'true'
    needs:
      - classify
      - resolve-hamlet
    steps:
      - uses: actions/checkout@v6

      - name: Checkout exact Hamlet source
        uses: actions/checkout@v6
        with:
          repository: hamlet-org/hamlet
          ref: ${{ needs.resolve-hamlet.outputs.git_commit }}
          token: ${{ secrets.HAMLET_READ_TOKEN || github.token }}
          path: hamlet-source
          fetch-depth: 1
          persist-credentials: false

      - uses: ./.github/actions/setup-ocaml
        with:
          ocaml-compiler: ${{ env.OCAML_COMPILER }}

      - run: opam pin add -y dune ${{ env.MINIMUM_DUNE }}

      - name: Ensure exact Hamlet source dependencies
        shell: bash
        run: |
          set -euo pipefail
          opam pin add -y --no-action hamlet "$GITHUB_WORKSPACE/hamlet-source"
          opam pin add -y --no-action ppx_hamlet "$GITHUB_WORKSPACE/hamlet-source"
          opam install -y hamlet ppx_hamlet
          opam install -y --ignore-pin-depends . --deps-only

      - run: test "$(opam exec -- dune --version)" = "${{ env.MINIMUM_DUNE }}"

      # test dependencies include OCaml-LSP, whose current Dune dependency is
      # newer than this compatibility floor. @install excludes test stanzas;
      # the modern-Dune job runs the full behavioral contract.
      - run: opam exec -- dune build @install

  build-macos:
    name: build (macos-latest, 5.5.0)
    if: ${{ vars.ENABLE_MACOS_CI == 'true' && needs.classify.outputs.docs_only != 'true' }}
    runs-on: macos-latest
    needs:
      - classify
      - resolve-hamlet
    steps:
      - uses: actions/checkout@v6

      - name: Checkout exact Hamlet source
        uses: actions/checkout@v6
        with:
          repository: hamlet-org/hamlet
          ref: ${{ needs.resolve-hamlet.outputs.git_commit }}
          token: ${{ secrets.HAMLET_READ_TOKEN || github.token }}
          path: hamlet-source
          fetch-depth: 1
          persist-credentials: false

      - uses: ./.github/actions/setup-ocaml
        with:
          ocaml-compiler: ${{ env.OCAML_COMPILER }}

      - name: Ensure exact Hamlet source dependencies
        shell: bash
        run: |
          set -euo pipefail
          opam pin add -y --no-action hamlet "$GITHUB_WORKSPACE/hamlet-source"
          opam pin add -y --no-action ppx_hamlet "$GITHUB_WORKSPACE/hamlet-source"
          opam install -y hamlet ppx_hamlet
          opam install -y --ignore-pin-depends . --deps-only --with-test --with-doc --with-dev-setup
          opam exec -- make all

  quality:
    runs-on: ubuntu-latest
    if: needs.classify.outputs.docs_only != 'true'
    needs:
      - classify
      - resolve-hamlet
      - ensure-ci-image
    container:
      image: ${{ needs.ensure-ci-image.outputs.image }}
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v6

      - name: Checkout exact Hamlet source
        uses: actions/checkout@v6
        with:
          repository: hamlet-org/hamlet
          ref: ${{ needs.resolve-hamlet.outputs.git_commit }}
          token: ${{ secrets.HAMLET_READ_TOKEN || github.token }}
          path: hamlet-source
          fetch-depth: 1
          persist-credentials: false

      - run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$GITHUB_WORKSPACE/hamlet-source"

      - name: Ensure exact Hamlet source dependencies
        shell: bash
        run: |
          set -euo pipefail
          opam pin add -y --no-action hamlet "$GITHUB_WORKSPACE/hamlet-source"
          opam pin add -y --no-action ppx_hamlet "$GITHUB_WORKSPACE/hamlet-source"
          opam install -y hamlet ppx_hamlet
          opam install -y --ignore-pin-depends . --deps-only --with-test --with-doc --with-dev-setup

      - run: make fmt doc opam
