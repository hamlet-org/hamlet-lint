(** Incomplete polymorphic-variant observations used only by private Typedtree
    support. Raw compiler-libs inspection stays in the OCaml 5.5 adapter. These
    tag names must never be promoted to exact subtraction proofs. *)

let present_tags = Hamlet_subtractor_compiler_compat_505.present_variant_labels
